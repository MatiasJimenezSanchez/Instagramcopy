import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 24
  },
  top_bar: {
    marginTop: 30,
  },
  stories_container: {
    marginTop: 30, 
  },
  posts_container: {
    marginTop: 30,
  }
});

export default styles;
